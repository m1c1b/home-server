apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: immich-data-claim
  namespace: immich
spec:
  volumeName: immich-data
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Ti
  storageClassName: immich-data