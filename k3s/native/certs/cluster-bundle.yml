---
apiVersion: trust.cert-manager.io/v1alpha1
kind: Bundle
metadata:
  name: trust-bundle
  namespace: cert-manager
spec:
  sources:
    - useDefaultCAs: true

    - secret:
        name: "root-ca"
        key: "tls.crt"
  target:
    configMap:
      key: "ca-trust-bundle.pem"
    namespaceSelector:
      matchLabels:
        trust: "enabled"
    additionalFormats:
      pkcs12:
        key: "bundle.p12"
        password: 'changeit'